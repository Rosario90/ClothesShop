<div>
    <h2> {{ order.id }} </h2>

    <h3> {{ order.date }} </h3>

    <h2> {{ order.customer }} </h2>
    {% for line in order.contents %}
        <div>
            <h2> {{ line.product.name }} - {{ line.quantity }} </h2>
        </div>
    {% endfor %}
</div>